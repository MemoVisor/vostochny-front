{
  "name": "memovisor-frontend",
  "version": "1.0.0",
  "scripts": {
    "start": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "lint": "eslint -c .eslintrc.json src --ext .js,.ts",
    "stylelint": "npx stylelint 'src/**/*.css'"
  },
  "devDependencies": {
    "@chernodub/eslint-config": "^2.0.2",
    "eslint": "^8.7.0",
    "stylelint": "^14.3.0",
    "stylelint-config-recommended": "^6.0.0",
    "typescript": "^4.5.5",
    "vite": "^2.7.2"
  }
}
